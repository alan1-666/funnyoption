后端开发
Golang: 熟练掌握并发模型与性能调优，具备 pprof 驱动的全链路瓶颈定位与优化经验；熟悉 SPSC 无锁队列、cache-line padding、slab 对象池、bitmap 位运算等低延迟编程范式
微服务与中间件: 熟悉 Gin、go-zero、gRPC、WebSocket，具备多服务协作、接口版本管理及 protobuf 契约演进经验
数据存储: 熟悉 MySQL、PostgreSQL、Redis，具备索引调优、multi-row 批量写入优化、缓存一致性保障与分布式锁落地经验
消息与异步: 熟悉 Kafka 有序消费与多 partition 并行消费、LZ4 压缩批量发布、幂等设计与死信处理；了解 Aeron IPC/UDP 传输模型及其在金融撮合领域的应用
运维与可观测性: 熟悉 Docker、Linux 线上排查、Prometheus 监控告警；具备 pprof / benchmark 性能分析实践经验
区块链 / 交易基础设施
钱包与资金业务: 熟悉多链钱包核心流程（充值扫链 / 提现 / 归集 / 热冷划转 / reorg 检测），具备多租户钱包 SaaS 架构设计与落地经验
签名与密钥管理: 熟悉 HD 钱包（BIP32/BIP44）、签名服务分层架构；了解 HSM / MPC 钱包原理
EVM合约接入: 熟悉 Solidity 及 Anchor（Solana），能完成合约调用封装、事件解析、EIP-1559 交易构造及链上链下状态对齐
Solana生态: 熟悉账户模型、SPL Token / Token-2022；具备 Raydium / Pump.fun 等 DEX 协议集成经验；了解 CU 估算、Jito MEV 及 anti-MEV 策略
 项目经历
2025.12
-
至今
Seersmarket预测市场
Go后端组长
Seers是面向加密货币价格、体育赛事、热点事件等场景，构建Web3预测市场平台，覆盖市场创建、订单簿交易、实时行情、限价/市价下单、自动/人工裁决、资金结算、邀请分润与消息通知等完整业务闭环。
高性能撮合引擎（借鉴Aeron设计理念）
主导撮合引擎架构设计，采用InputGateway → BookEngine → OutputDispatcher三级流水线，借鉴Aeron single-writer原则实现热路径零I/O、接近零GC；每book独立SPSC无锁环形缓冲区 + Aeron风格渐进退避IdleStrategy；实测单book撮合127万 ops/s
利用预测市场价格有界的特性，以固定数组O(1)寻址 + bitmap位运算跳跃 + 侵入式链表FIFO + slab对象池，实现价格-时间优先撮合，替代传统红黑树方案；pprof发现Snapshot线性扫描占83% CPU后改为bitmap遍历，关键路径提速7–9x
Gateway层ConsumerGroup多partition并行消费 + Dispatcher层multi-row INSERT并发worker分片保序 + LZ4压缩批量发布；通过pprof逐层定位打通后，E2E吞吐从56提升至3400 trades/s，提升61倍
支持GTC / IOC / FOK（两阶段预检）/ POST_ONLY + 三种STP自成交保护 + Amend Order；实现确定性TradeID + Shadow模式HA
核心业务实现
负责 20+ 核心微服务规划与开发，独立交付订单、撮合、结算、账本、行情推送等核心链路，支撑平台从 0 到上线全周期
设计 Kafka 驱动的交易链路（下单 → 冻结 → 撮合 → 账变 → 结算），matching 作为 book 唯一写入者，分区键对齐保证全序；TradeID 幂等防重复消费
设计复式记账账本：Account 层管余额/冻结，Ledger 层追加型日志强制借贷平衡；对账报告自动比对链上链下状态，输出 BALANCED / DRIFT
负责钱包 SaaS 衔接，实现 mirror-accounting 模型统一撮合与资金视图；多币种充值自动兑换（Binance 实时价格 → USDT 折合）
参与 ZK-Rollup 批次编排（record → publish → accept 三阶段提交 + Groth16 证明 + 失败自动重试），推动链下状态与链上锚定闭环
2023.05
-
2025.10
多链聚合钱包SaaS系统
Go后端开发
面向集团内多个 Web3 项目提供统一钱包基础设施，覆盖 6+ 条链、多租户隔离，承担链上资产与平台账户体系之间的底层连接。
核心业务实现
主导五大微服务架构设计（api-gateway / wallet-core / chain-gateway / sign-service / scan-service），为多个业务项目提供统一的地址管理、充值、提现、归集与账务映射能力
主导签名服务重构，搭建 gRPC signer + custody provider 分层架构，密钥派生与业务解耦；实现 HD 钱包模型支持 ECDSA / EdDSA 双算法，覆盖 Ethereum、BSC、Polygon、Arbitrum、Solana、Tron 等多链
实现 eth_getLogs 批量 ERC-20 扫链 + parentHash reorg 检测 + RPC 指数退避重试 + outbox 行级锁防并发，保障充值链路可靠性
设计提现异步队列与 stuck tx 自动加速（nonce replacement / speed-up）；冷热钱包两层归集，按余额阈值动态路由
2022.07
-
2023.03
Solana Dex聚合
Go后端开发
Solana DEX 数据聚合与交易系统，覆盖链上数据采集、多协议解析、价格发现、交易构造等核心功能。
核心业务实现
实现多协议统一解析框架，覆盖 Raydium（AMM/CLMM/CPMM）、Pump.fun、Token-2022；支持市价单、限价单、追踪止损等交易场景
基于 Jupiter 路径选择 + Jito 优先费 + 滑点重试优化交易成功率；构建基于 slot 的并行处理架构，支持存量/增量/异常 slot 分流
基于 Redis + WebSocket 构建实时数据分发链路，支撑链上监控与交易推送
 教育经历
2018.09
-
2022.07
浙江科技大学
电气工程及其自动化
统招本科
 工作经历
2023.05
-
至今
Foya科技
Golang后端工程师
主导预测市场撮合引擎 + 多链钱包 SaaS 两条核心产品线的后端架构与交付
2022.07
-
2023.03
Ave.ai
Golang后端工程师
负责 Solana DEX 聚合平台链上数据采集、多协议解析与交易构造
语言能力
德语 B2、英语 CET-4